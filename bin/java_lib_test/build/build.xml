<project name="RalphJavaLibTest" default="compile" basedir=".">
  
  <description>
    Test cases for java libraries.
  </description>

  <dirname
    property="lib_test_dir"
    file="${ant.file.RalphJavaLibTest}"
  />

  <!-- global properties for this build file -->
  <property name="build.dir" location="${lib_test_dir}/classes"/>
  <property name="src.dir" location="${lib_test_dir}/../test_src"/>
  <property
      name="lib_build_path"
      location="${lib_test_dir}/../../../ralph/java_lib/build"/>
  <property
      name="lib_class_path" location="${lib_build_path}/classes/"/>
  <property
      name="proto_class_path" location="${lib_build_path}/../protobuf-java-2.5.0.jar"/>
  
  <!-- set up build directories for test -->
  <target name="init" description="setup build directories">
    <echo message="Setting up build directory for tests"/>
    <mkdir dir="${build.dir}"/>
  </target>

  <!-- compile java libraries -->
  <target name="compile_libs" depends="init" description="compile library files">
    <ant antfile="${lib_build_path}/build.xml" target="compile_all" />  
  </target>

  <!-- compile all library tests -->
  <target
      name="compile_all"
      depends="init,compile_libs"
      description="compile library and test files">

    <javac
        srcdir="${src.dir}"
        destdir="${build.dir}"
        classpath="${lib_class_path}:${proto_class_path}"
        debug="true"
        debuglevel="lines,vars,source"
        includeantruntime="false" />
  </target>
  

  <!-- Run all tests specified -->
  <target
      name="run_all"
      depends=
         "run_WriteReadTVar,run_PartnersNoConflict,
         run_PartnersModifyRemoteState,run_NonAtomic,run_MapTVar,run_ListTVar,
         run_ExtendedListTVar,run_UpdateSerializer,run_VersionQuerier">
  </target>
  
  <!-- List individual tests -->
  <target
      name="run_UpdateSerializer"
      depends="compile_all"
      description="Run update serialization test.">
    <java
        dir="${build.dir}"
        classname="java_lib_test.UpdateSerializer"
        fork="yes"
        classpath="${build.dir}:${proto_class_path}:
                   ${build.dir}/ralph_version_protobuffs/*.class:">
        <arg line="${args}"/>
    </java>
  </target>

  
  <target
      name="run_VersionQuerier"
      depends="compile_all"
      description="Run version querier test.">
    <java
        dir="${build.dir}"
        classname="java_lib_test.VersionQuerier"
        fork="yes"
        classpath="${build.dir}:${proto_class_path}:
                   ${build.dir}/ralph_version_protobuffs/*.class:">
        <arg line="${args}"/>
    </java>
  </target>

  <target
      name="run_WriteReadTVar"
      depends="compile_all"
      description="Run tvar tests.">
    <java
        dir="${build.dir}"
        classname="java_lib_test.WriteReadTVar"
        fork="yes"
        classpath="${build.dir}:${proto_class_path}">
        <arg line="${args}"/>
    </java>
  </target>

  <target
      name="run_PartnersNoConflict"
      depends="compile_all"
      description=
        "Checks that rpcs issued between partners without conflict send and return args.">
    <java
        dir="${build.dir}"
        classname="java_lib_test.PartnersNoConflict"
        fork="yes"
        classpath="${build.dir}:${proto_class_path}">
        <arg line="${args}"/>
    </java>
  </target>


  <target
      name="run_PartnersModifyRemoteState"
      depends="compile_all"
      description="Increments value on remote endpoint.">
    <java
        dir="${build.dir}"
        classname="java_lib_test.PartnersModifyRemoteState"
        fork="yes"
        classpath="${build.dir}:${proto_class_path}">
        <arg line="${args}"/>
    </java>
  </target>

    <target
      name="run_MapTVar"
      depends="compile_all"
      description="Tests can use a map as a tvar.">
    <java
        dir="${build.dir}"
        classname="java_lib_test.MapTVarConflict"
        fork="yes"
        classpath="${build.dir}:${proto_class_path}">
        <arg line="${args}"/>
    </java>
  </target>

    <target
      name="run_ListTVar"
      depends="compile_all"
      description="Tests can use a list as a tvar.">
    <java
        dir="${build.dir}"
        classname="java_lib_test.ListTVarConflict"
        fork="yes"
        classpath="${build.dir}:${proto_class_path}">
        <arg line="${args}"/>
    </java>
  </target>

    <target
      name="run_ExtendedListTVar"
      depends="compile_all"
      description="Tests can extend a tvar list.">
    <java
        dir="${build.dir}"
        classname="java_lib_test.ExtendedListTVar"
        fork="yes"
        classpath="${build.dir}:${proto_class_path}">
        <arg line="${args}"/>
    </java>
  </target>

    <target
      name="run_NonAtomic"
      depends="compile_all"
      description="Tries to non atomic event.">
    <java
        dir="${build.dir}"
        classname="java_lib_test.NonAtomic"
        fork="yes"
        classpath="${build.dir}:${proto_class_path}">
        <arg line="${args}"/>
    </java>
  </target>

  
  
  <!-- Delete the build directory -->
  <target name="clean" description="get rid of build dir">
    <delete dir="${build.dir}"/>
  </target>


  
  <target
      name="clean_all"
      depends="clean"
      description="get rid of build dir and all built libs">
    
    <ant antfile="${lib_build_path}/build.xml" target="clean" />
    
  </target>

  
</project>
