import "util.proto";

// FIXME: for all numeric types, add [packed = true];

option java_package = "ralph_protobuffs";
option java_outer_classname = "VariablesProto";


message Variables
{
    message List
    {
        repeated Any list_values = 1;
    }

    message Map
    {
        repeated Any map_indices = 1;
        repeated Any map_values = 2;
    }

    message Any
    {
        // for case of data internal to a map/list/struct, use a
        // garbage var name.
        required string var_name = 1;
        // true if when done with request, sends the variable back.
        required bool reference = 2;
        optional double num = 3;
        optional string text = 4;
        optional bool true_false = 5;
        optional List list = 6;
        optional Map map = 7;
        optional Map struct = 8;
    }

    repeated Any vars = 1;
}


